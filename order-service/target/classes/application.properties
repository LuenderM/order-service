# configurando conex�o com MySql!

# url do banco db - Obrigat�rio!
spring.datasource.url = jdbc:mysql://localhost:3306/order_service_db?useTimezone=true&serverTimezone=UTC

# username
spring.datasource.username = root

# senha
spring.datasource.password = Luender@2025

# permitir que o Hibernate crie e atualize tabelas automaticamente
spring.jpa.hibernate.ddl-auto=validate

# ativar uso do flyway
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

# mostrar no console as instru��es SQL geradas
spring.jpa.show_sql = true

# dialeto do SQL do banco
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true
